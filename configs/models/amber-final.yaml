family: amber
provider: IFM
checkpoint: IFM/Amber
revision: main
parameters_total: "7B"
architecture: dense
training_stage: base
parent_checkpoint: null

transparency:
  tier: T1
  pretraining_data_reconstructable: true
  training_code_available: true
  intermediate_checkpoints_available: true

license:
  name: apache-2.0
  gated: false

inference:
  backend: vllm
  dtype: bfloat16
  tensor_parallel_size: 1

locked_anchor: true
scientific_role: >
  Locked anchor B (final Amber checkpoint). Org renamed LLM360 -> IFM on HF
  (confirmed rename, not a substitution).
